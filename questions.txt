7.Write the difference between the local variable and global variable
: the global variable is a variable its scope is the whole program
but the local variable is a variable its scope is the function where it declared 
